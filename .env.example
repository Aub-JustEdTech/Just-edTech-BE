# ====================================
# Application Configuration
# ====================================
API_V1_STR=/api/v1
PROJECT_NAME=Just-EdTech

# ====================================
# Security
# ====================================
SECRET_KEY=your-secret-key-change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# ====================================
# Database Configuration (PostgreSQL)
# ====================================
# Local dev: Postgres runs on the host machine, not in Docker.
# Use host.docker.internal so Docker containers can reach the host's Postgres.
# Production: set to your RDS endpoint (e.g. mydb.xxxx.rds.amazonaws.com).
POSTGRES_SERVER=host.docker.internal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=just_edtech
POSTGRES_PORT=5432
# Set to "require" for RDS/production, leave empty for local dev.
POSTGRES_SSLMODE=

# ====================================
# Redis Configuration
# ====================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REFRESH_TOKEN_EXPIRE_DAYS=30

# ====================================
# CORS Configuration
# ====================================
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8080

# ====================================
# RAG Configuration
# ====================================
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_TOKENS=4000

# ====================================
# Vector Store Configuration
# ====================================
VECTOR_STORE_TYPE=chroma
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION_PREFIX=tenant

# Future: Pinecone Configuration (Optional)
# PINECONE_API_KEY=your-pinecone-api-key
# PINECONE_ENVIRONMENT=your-pinecone-environment

# Future: Weaviate Configuration (Optional)
# WEAVIATE_URL=http://localhost:8080

# ====================================
# LLM API Configuration (OpenRouter)
# ====================================
LLM_API_PROVIDER=openrouter
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_HTTP_REFERER=http://localhost:8000
OPENROUTER_APP_NAME=Just-EdTech
OPENROUTER_BATCH_CONCURRENCY=10
# Optional fallback when using openrouter, or primary key when LLM_API_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=openai/text-embedding-3-small

# ====================================
# AWS S3 Configuration
# ====================================
S3_BUCKET_NAME=your-s3-bucket-name
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# ====================================
# Schema-driven crawler POC (experiment branch)
# Model used by scripts/school_data/schema_crawl_poc. Defaults to the
# heatmap doc-classifier model when unset.
# ====================================
SCHOOL_SCRAPER_LLM_PAGE_CLASSIFIER_MODEL=openai/gpt-4o-mini

# ====================================
# Document Processing Configuration
# ====================================
MAX_FILE_SIZE_MB=50
ALLOWED_DOCUMENT_TYPES=[".pdf", ".md", ".txt", ".docx"]
TEMP_UPLOAD_DIR=./temp_uploads
